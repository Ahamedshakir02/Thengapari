name: b2b
description: "ThengaPari — B2B Portal app."
publish_to: 'none'
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  core: ^0.0.1

  flutter_riverpod: ^3.3.1
  go_router: ^17.3.0
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  hive_flutter: ^1.1.0
  fl_chart: ^1.2.0
  share_plus: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
