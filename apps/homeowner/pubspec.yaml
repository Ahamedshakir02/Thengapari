name: homeowner
description: "ThengaPari — Homeowner app."
publish_to: 'none'
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Shared code
  core: ^0.0.1

  # Direct dependencies used by homeowner screens / entrypoint
  flutter_riverpod: ^3.3.1
  go_router: ^17.3.0
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  cloud_firestore: ^6.5.0
  hive_flutter: ^1.1.0
  flutter_svg: ^2.3.0
  razorpay_flutter: ^1.4.5
  share_plus: ^13.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
